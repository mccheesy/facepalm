# 2-space indent, spaces only — the inherited ZMK/knucklehead style for this repo.
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown uses trailing spaces for hard line breaks
[*.md]
trim_trailing_whitespace = false
